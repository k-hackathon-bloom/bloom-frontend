import React from 'react';
import StyledText from '@components/common/StyledText';
import ScreenLayout from '@screens/ScreenLayout';

const BottleMessage = () => {
  return (
    <ScreenLayout>
      <StyledText>Bottle Message Screen</StyledText>
    </ScreenLayout>
  );
};

export default BottleMessage;
